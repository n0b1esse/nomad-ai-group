# Netlify: HTTPS и сертификаты подключаются автоматически для *.netlify.app и кастомных доменов.
# Многостраничный статический сайт — SPA-фолбэк не используется.
# При необходимости редиректа со старого домена раскомментируйте и укажите хосты:
# https://old-domain.com/*  https://new-domain.com/:splat  301!
